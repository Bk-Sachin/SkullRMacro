<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <!-- Optional: If you WANT to explicitly manage files, disable defaults -->
    <!-- <EnableDefaultCompileItems>false</EnableDefaultCompileItems> -->
    <!-- <EnableDefaultPageItems>false</EnableDefaultPageItems> -->
  </PropertyGroup>

  <!-- ItemGroup with explicit includes removed as they are handled by the SDK defaults -->
  <!-- 
  <ItemGroup>
    <ApplicationDefinition Include="App.xaml" /> 
    <Compile Include="App.xaml.cs" /> 
    <Compile Include="AssemblyInfo.cs" /> 
    <Compile Include="MainWindow.xaml.cs" /> 
    <Compile Include="MacroEditor.xaml.cs">
      <DependentUpon>MacroEditor.xaml</DependentUpon> 
    </Compile>
    <Compile Include="MacroStepViewModel.cs" /> 
    <Compile Include="MousePartProperties.cs" /> 
    <Page Include="MainWindow.xaml" /> 
    <Page Include="MacroEditor.xaml">
      <SubType>Designer</SubType> 
      <Generator>MSBuild:Compile</Generator> 
    </Page>
  </ItemGroup>
  -->

</Project>
